name: Deploy service-syncer to Render
on:
  pull_request:
    branches:
      - main
    types:
      - closed
    paths:
      - 'apps/service-syncer/**'
      - 'apps/worker-pdaotao-consumer/**'
      - '.github/workflows/service-syncer-deployment.yml'
jobs:
  deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install pnpm
        uses: pnpm/action-setup@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile
      - name: Build Syncer Service
        run: pnpm --filter service-syncer run build
      - name: Deploy to Render
        uses: johnbeynon/render-deploy-action@v0.0.8
        with:
          service-id: ${{ secrets.RENDER_PROCESSOR_SERVICE_ID }}
          api-key: ${{ secrets.RENDER_API_KEY }}
